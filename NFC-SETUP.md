# NFC 专属编号 + 电子签名 · 部署指南

《五百个冬天》实体专辑 NFC 系统（静态方案，零后端）。

## 工作原理

```
每张专辑的贴片写入一个带编号的唯一网址
  ?nfc=official&no=0001   ← 第 1 张
  ?nfc=official&no=0136   ← 第 136 张
  ...
粉丝手机碰一碰贴片
  → 打开对应网址
  → 展示这张专辑的专属编号（No. 0136 / 500）
  → 展示对应编号的 lilwukong 电子签名图
  → 记录「本机第 N 次查看」
编号超出 1 ~ 500 → 显示「无法验证的入口」
```

纯静态页面，不需要服务器和数据库，推到 GitHub Pages 即完成上线。

## 上线两步

### 1. 部署页面

把本目录整体推到 GitHub 仓库 `lilwukong-500-winters`（保留 `.nojekyll`），GitHub Pages 自动更新。

### 2. 写贴片

- 文件：`tags/nfc-urls.txt`（500 行，第 1 行 = No. 0001，依次类推）
- 工具：手机装「NFC Tools」（支持批量/逐行写入），或买贴片时让卖家代写（直接把这个文件发他，网址无密钥，不怕泄露）
- 贴片型号：NTAG213 即可（网址约 78 字节，容量 144 字节够用）
- 写完逐张抽检：扫第几张就该显示 No. 几

发行量不是 500 就重新生成：`python tools/generate_tags.py 300`，同时把 `claim.js` 顶部 `TOTAL_EDITIONS` 改成同一个数。

## 编号与签名图的对应关系

签名图共 6 张（`assets/signatures/sig-01.png ~ sig-06.png`），按编号循环分配（No. 0001 → sig-01，No. 0007 → sig-01 …）。想加新图：往目录放 `sig-07.png` 等，并把 `claim.js` 里 `SIGNATURE_POOL` 改成新数量。

## 本地预览

```bash
npm install
npm run dev -- --port 7100
```

然后访问 `http://localhost:7100/?no=0136`（和真实贴片网址同格式）。

## 防伪说明（请知悉）

静态方案的编号是公开的，任何人改网址数字都能看到别的编号，防伪只靠页面提示「认准官方域名」。对粉丝收藏场景通常够用。如果以后想要「首次扫码独占领取、复制网址无效」的强防伪，需要加后端（Supabase + NTAG 424 DNA 贴片），届时重写贴片即可，页面结构不用大改。
